import {SetSwapOptimismTokens} from '../index';

describe('arent empty', () => {
  test('should add up positive numbers', async () => {
    // expect(SetSwapOptimismTokens.length).toBeGreaterThan(0);
  });
});
